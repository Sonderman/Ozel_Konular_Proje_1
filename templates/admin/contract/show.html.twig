{% extends 'base.html.twig' %}

{% block title %}Contract{% endblock %}

{% block body %}
    <h1>Contract</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ contract.id }}</td>
            </tr>
            <tr>
                <th>Customer_id</th>
                <td>{{ contract.customerId }}</td>
            </tr>
            <tr>
                <th>Car_id</th>
                <td>{{ contract.carId }}</td>
            </tr>
            <tr>
                <th>Pick_up_date</th>
                <td>{{ contract.pickUpDate ? contract.pickUpDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Drop_off_date</th>
                <td>{{ contract.dropOffDate ? contract.dropOffDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Pick_up_location</th>
                <td>{{ contract.pickUpLocation }}</td>
            </tr>
            <tr>
                <th>Drop_off_location</th>
                <td>{{ contract.dropOffLocation }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_contract_index') }}">back to list</a>

    <a href="{{ path('admin_contract_edit', {'id': contract.id}) }}">edit</a>

    {{ include('admin/contract/_delete_form.html.twig') }}
{% endblock %}
